# Server
NODE_ENV=development
PORT=3000

# Timezone
TZ=America/Bogota

# API Security - CAMBIAR EN PRODUCCIÓN
API_TOKEN=mi-token-secreto-2025

# PostgreSQL Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=new_year_messages

# TikTok Business API (opcional)
# TIKTOK_API_URL=https://open.tiktokapis.com/v2
# TIKTOK_ACCESS_TOKEN=your_tiktok_access_token

# Cron Expression (default: every 5 minutes)
# Format: minutes hours day-of-month month day-of-week
DEFAULT_CRON_EXPRESSION=*/5 * * * *

# NOTA: WhatsApp se conecta escaneando el código QR
# que aparece en la consola al iniciar la aplicación.
# La sesión se guarda en ./whatsapp-session/
